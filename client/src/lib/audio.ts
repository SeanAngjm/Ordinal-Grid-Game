// Audio functionality disabled per user request
export const speak = (text: string) => {
  // Disabled
};

export const playSound = (type: 'correct' | 'wrong' | 'start' | 'pop') => {
  // Disabled
};
